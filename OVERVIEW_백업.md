# 환경계획(EP) 통합 모니터링 대시보드 — 전체 안내서

> **이 문서는 누구를 위한 것인가**
> 웹 개발을 모르는 사람이 이 사이트를 처음부터 이해할 수 있도록 쓴 설명서입니다.
> ① 이 사이트가 무엇이고 ② 어떤 기능이 있으며 ③ **AI 개발 도구인 Claude Code로 어떻게 만들어졌는지**를
> 순서대로 다룹니다. 전문 용어는 4장 「용어 사전」에서 모두 풀어 씁니다.

| 항목 | 내용 |
|---|---|
| 정식 명칭 | 환경계획 통합 모니터링 대시보드 |
| 약칭 | EP Dashboard (EP = Environmental Planning, 환경계획) |
| 문서 작성일 | 2026-08-19 |
| 대상 버전 | v1.0 (모니터링 영역 3분할 + EP 브랜딩 반영본) |
| 저장소 | `/home/dyjin/work/ep_dashboard` (branch `master`) |
| 개발 방식 | Claude Code(AI 페어 프로그래밍) 기반, 사람이 요구·검수 / AI가 설계·구현 |

---

## 목차

| 장 | 제목 | 이런 게 궁금하면 |
|---|---|---|
| 1 | [한 문단 요약](#1-한-문단-요약) | "그래서 이게 뭔데?" |
| 2 | [지금 들어 있는 것](#2-지금-들어-있는-것-숫자로-보기) | 데이터 규모 |
| 3 | [왜 만들었나](#3-왜-만들었나) | 배경·문제의식 |
| 4 | [비개발자를 위한 용어 사전](#4-비개발자를-위한-용어-사전) | 용어가 어려울 때 |
| 5 | [사이트 전체 지도](#5-사이트-전체-지도) | 화면이 몇 개인지 |
| 6 | [데이터의 4단 계층](#6-데이터의-4단-계층) | 정보 구조 |
| 7 | [공개 화면 기능 상세](#7-공개-화면-기능-상세) | 일반 이용자가 보는 것 |
| 8 | [관리자 화면 기능 상세](#8-관리자-화면-기능-상세) | 담당자가 쓰는 것 |
| 9 | [달성도 계산 규칙](#9-달성도-계산-규칙-이-사이트의-심장) | "순조/지연은 어떻게 정해지나" |
| 10 | [색상 체계](#10-색상-체계) | 화면 색의 의미 |
| 11 | [데이터베이스 구조](#11-데이터베이스-구조) | 어디에 무엇이 저장되나 |
| 12 | [CSV 왕복](#12-csv-왕복-엑셀과-주고받기) | 엑셀로 대량 작업 |
| 13 | [로그인·보안·수정 이력](#13-로그인보안수정-이력) | 누가 고칠 수 있나 |
| 14 | [**Claude Code로 어떻게 만들었나**](#14-claude-code로-어떻게-만들었나) | ★ 개발 과정 전체 |
| 15 | [기술 스택](#15-기술-스택-무엇으로-만들어졌나) | 사용된 기술 |
| 16 | [설치·실행](#16-설치실행) | 직접 띄워보기 |
| 17 | [자주 하는 작업 레시피](#17-자주-하는-작업-레시피) | 실무 매뉴얼 |
| 18 | [주의사항·한계](#18-주의사항과-한계) | 공개 전 확인 |
| 19 | [앞으로 할 수 있는 것](#19-앞으로-할-수-있는-것) | 로드맵 |
| 20 | [문서 지도](#20-문서-지도) | 다른 문서 안내 |

---

## 1. 한 문단 요약

환경계획의 이행 상황을 **웹사이트 하나에서** 보여주는 대시보드입니다.
지표마다 「기준값 → 최신값 → 목표값」을 저장해 두면 사이트가 자동으로 **달성률을 계산하고
달성 / 순조 / 지연 / 악화 / 모니터링 / 자료없음 6가지 상태로 판정**해 색과 배지로 표시합니다.
담당자는 개발자 도움 없이 **관리자 화면에서 지표를 추가하고 연도별 실적값을 입력**할 수 있고,
엑셀에서 만든 표를 **CSV로 한 번에 올릴 수도** 있습니다. 모든 수정은 이력에 남습니다.

핵심 설계 원칙이 하나 있습니다 — **지금 들어 있는 예시 지표(K-SDGs 환경분야)는 코드 어디에도
박혀 있지 않습니다.** 나중에 자체 환경계획 지표가 확정되면 프로그램을 고치지 않고
**관리자 화면에서 내용만 갈아끼우면** 그대로 자체 계획 대시보드가 됩니다.
심지어 「목표 / 세부목표 / 지표」라는 **용어 자체도 설정 화면에서 바꿀 수 있습니다.**

---

## 2. 지금 들어 있는 것 (숫자로 보기)

현재 데이터베이스에 담긴 실제 수량입니다.

| 구분 | 수량 | 설명 |
|---|---:|---|
| 모니터링 영역 | 3 | 계획이행 / 환경상태 / 환경체감 |
| 목표 | 9 | 영역당 3개씩 |
| 세부목표 | 20 | |
| 지표 | 32 | |
| 연도별 실적값 | 154 | 2018~2024년 |
| 이행과제 | 10 | 계획이행 영역에만 존재 |
| 사이트 설정 항목 | 12 | 제목·계층 명칭·기관명 등 |
| 수정 이력 | 16건 | 사용할수록 쌓임 |

### 모니터링 영역 3개

| 영역 | 주소 | 색 | 아이콘 | 다루는 것 |
|---|---|---|---|---|
| 계획이행 모니터링 | `/plan` | 짙은 파랑 `#1864AB` | 🧭 | 계획의 목표·세부목표별 이행 상황 (지표 13개 + 이행과제 10건) |
| 환경상태 모니터링 | `/state` | 청록 `#0CA678` | 🌍 | 대기·물·생활/자연환경의 실제 관측치 (지표 10개) |
| 환경체감 모니터링 | `/perception` | 보라 `#7048E8` | 💬 | 시민 만족도·불편 경험·인식 조사 (지표 9개) |

목표 9개는 다음과 같습니다.

```
계획이행(plan)     ├ G13 기후변화와 대응      ├ G14 해양생태계 보전    └ G15 육상생태계 보전
환경상태(state)    ├ S1  대기환경             ├ S2  물환경            └ S3  생활·자연환경
환경체감(perception)├ P1  환경 만족도          ├ P2  환경 불편 경험     └ P3  환경 인식과 참여
```

지표 32개의 현재 상태 분포는 **달성 3 · 순조 11 · 지연 11 · 악화 3 · 모니터링 3 · 자료없음 1**,
전체 평균 달성도는 **43%** 입니다. (이 숫자들은 예시 데이터 기준입니다 — 18장 참고)

---

## 3. 왜 만들었나

### 3.1 기존 방식의 문제

환경계획의 이행 상황은 보통 **연 1회 한글/엑셀 보고서**로 정리됩니다. 여기엔 세 가지 문제가 있습니다.

| 문제 | 구체적으로 |
|---|---|
| **열람성** | 기준값·목표값·최신값·달성률이 문서 곳곳에 흩어져 있어 "지금 뭐가 지연 중인지"가 한눈에 안 보인다 |
| **갱신성** | 통계가 새로 나와도 문서를 다시 만들어야 반영된다 |
| **추적성** | 누가 언제 어떤 수치를 고쳤는지 기록이 남지 않는다 |

### 3.2 참고한 모델

EU JRC(Joint Research Centre)의 두 대시보드를 참고했고, **둘을 한 앱에 합쳤습니다.**

| 참고 사이트 | 여기서 가져온 것 |
|---|---|
| [EU BDS Actions Tracker](https://dopa.jrc.ec.europa.eu/kcbd/actions-tracker/) | 상단 상태별 집계 카드, 주제별 그룹 아코디언, 상태·기한 칩, 요약문 + 근거 링크 |
| [EU BDS 2030 Dashboard](https://dopa.jrc.ec.europa.eu/kcbd/EUBDS2030-dashboard/) | 지표별 기준값·목표값·최신값·달성률, 시계열 차트, 메타데이터(출처·산출방법·갱신주기) |

### 3.3 왜 지금은 K-SDGs 예시 데이터인가

최종 목표는 **자체 환경계획 지표** 대시보드지만, 계획지표가 아직 확정되지 않았습니다.
그래서 국가 지속가능발전목표(K-SDGs) 환경분야(13 기후변화 · 14 해양생태계 · 15 육상생태계)로
**형식 견본**을 먼저 만들어 두고, 계획지표가 확정되면 코드 수정 없이 내용만 교체합니다.

> 2026-08-19 기준으로 **프로그램 안의 「K-SDGs」 표기는 모두 EP(환경계획)로 교체**되었고,
> K-SDGs는 이제 "예시로 들어 있는 데이터"의 출처일 뿐 브랜드가 아닙니다.

### 3.4 제품 목표

| # | 목표 | 달성했다고 볼 기준 |
|---|---|---|
| G1 | 이행 상황을 한 화면에서 파악 | 첫 화면 진입 3초 안에 영역별 평균 달성도·상태 분포·지연 지표 확인 |
| G2 | 개발자 없이 데이터 갱신 | 담당자가 관리자 화면만으로 지표 추가 → 실적값 입력 → 공개까지 완결 |
| G3 | 지표 체계 교체를 코드 수정 없이 | 계층 명칭 변경 + 데이터 교체만으로 자체 계획지표 대시보드로 전환 |
| G4 | 변경 추적 | 모든 쓰기 작업이 이력에 남아 무엇이 언제 바뀌었는지 조회 가능 |

---

## 4. 비개발자를 위한 용어 사전

이 문서(그리고 이 프로젝트의 다른 문서)에 나오는 용어를 미리 풀어 둡니다.

| 용어 | 쉬운 설명 |
|---|---|
| **웹앱 / 프론트엔드 / 백엔드** | 브라우저에 보이는 화면이 프론트엔드, 데이터를 저장·계산하는 뒤쪽이 백엔드. 이 프로젝트는 **하나의 프로그램이 둘 다** 담당합니다. |
| **Next.js** | 웹사이트를 만드는 대표적인 도구(프레임워크). 화면 만들기 + 서버 기능을 한 묶음으로 제공합니다. |
| **React** | 화면을 '부품(컴포넌트)' 단위로 조립하는 방식. 카드 하나, 배지 하나가 각각 부품입니다. |
| **TypeScript** | 자바스크립트에 "이 값은 숫자, 저 값은 글자" 같은 타입 검사를 붙인 언어. 오타·형 실수를 실행 전에 잡아줍니다. |
| **DB(데이터베이스)** | 데이터를 표 형태로 저장하는 곳. 이 프로젝트는 **SQLite** 라는 "파일 하나가 곧 데이터베이스"인 방식을 씁니다 (`prisma/dev.db`). |
| **Prisma** | DB를 다루는 도구. `schema.prisma` 파일에 표 구조를 적으면 DB가 그 모양대로 만들어집니다. |
| **스키마(schema)** | DB에 어떤 표(테이블)가 있고 각 표에 어떤 칸(컬럼)이 있는지 적어둔 설계도. |
| **시드(seed)** | 빈 DB에 초기 데이터를 부어 넣는 작업. `npm run db:seed` |
| **서버 액션(Server Action)** | 화면의 폼(입력창)에서 저장 버튼을 누르면 **서버에서 직접 실행되는 함수**. 별도 API를 만들지 않아도 되어 구조가 단순해집니다. |
| **CRUD** | Create(생성)·Read(조회)·Update(수정)·Delete(삭제). 관리자 화면이 하는 일 전부. |
| **CSV** | 쉼표로 칸을 나눈 텍스트 표 파일. 엑셀에서 그대로 열리고 저장됩니다. TSV는 탭으로 나눈 것. |
| **감사 로그(Audit Log)** | 누가 언제 무엇을 바꿨는지 남기는 기록장. 이 사이트는 모든 수정 작업이 자동으로 남습니다. |
| **쿠키(cookie)** | 브라우저가 들고 다니는 작은 출입증. 로그인 상태를 기억하는 데 씁니다. |
| **라우팅(routing)** | 주소(URL)와 화면을 연결하는 규칙. `/plan` 이라는 주소가 어떤 화면을 보여줄지 정하는 것. |
| **동적 세그먼트 `[track]`** | 주소의 한 칸을 변수로 두는 것. `/plan`·`/state`·`/perception`이 **한 벌의 화면 코드**로 처리됩니다. |
| **하드코딩(hard-coding)** | 값을 프로그램 안에 직접 박아 넣는 것. 이 프로젝트는 "K-SDGs·목표 개수·영역 코드"를 하드코딩하지 **않는 것**이 최우선 규칙입니다. |
| **Claude Code** | 터미널에서 대화하듯 지시하면 **코드를 직접 읽고 쓰고 실행·검증하는 AI 개발 도구**. 이 사이트를 만든 도구입니다. (14장) |

---

## 5. 사이트 전체 지도

화면은 크게 **공개 대시보드(누구나)** 와 **관리자(로그인 필요)** 둘로 나뉩니다.

```
누구나 볼 수 있는 화면
  /                         통합 현황 — 영역 3개를 나란히 비교
  /plan                     계획이행 영역 개요        ┐
  /plan/indicators          지표 목록 + 상세 드로어    │ 영역별로 동일한
  /plan/actions             이행과제 트래커            │ 4개 화면 세트가
  /plan/data                데이터·방법론              ┘ 자동 생성됨
  /state , /state/indicators , /state/data            (이행과제 0건 → 탭 자체가 안 보임)
  /perception , /perception/indicators , /perception/data
  /login                    관리자 로그인
  /api/export?type=...      CSV 내려받기 (주소로 직접 호출)

로그인해야 보이는 화면 (/admin)
  /admin                    개요 — 데이터 규모, 이행 현황, 점검 필요 항목, 최근 이력
  /admin/structure          영역 → 목표 → 세부목표 3단 계층 관리
  /admin/indicators         지표 목록 (필터·검색·공개 토글)
  /admin/indicators/new     지표 추가
  /admin/indicators/[id]    지표 편집 + 연도별 실적값 인라인 편집
  /admin/actions            이행과제 관리
  /admin/data               CSV 가져오기·내보내기
  /admin/config             사이트 설정 (제목·계층 명칭·기관명…)
  /admin/logs               수정 이력 (최근 300건)
  /admin/preview            미리보기 — 임시저장 항목까지 포함해 공개 화면과 동일하게 렌더
```

**중요한 점**: `/plan`·`/state`·`/perception` 이라는 주소는 프로그램에 적혀 있지 않습니다.
DB의 「모니터링 영역」 표에 있는 `code` 값에서 옵니다. 관리자에서 영역을 하나 더 추가하면
**상단 메뉴와 주소가 저절로 늘어납니다.**

---

## 6. 데이터의 4단 계층

모든 데이터는 아래 4단으로 정리되어 있고, 그 아래 연도별 실적값이 붙습니다.

```
모니터링 영역 (Track)          예) 환경상태 모니터링          → 주소·상단 메뉴가 됨
   └ 목표 (Goal)               예) S2 물환경                  → 색·아이콘을 가짐
        └ 세부목표 (Target)     예) S2-1 하천 수질
             └ 지표 (Indicator) 예) S2-1-1 BOD 농도            → 기준·목표·단위·출처
                  └ 실적값 (IndicatorValue)  2018:2.1  2019:2.0 …
                                              (연도 × 지역별로 1행)

이행과제 (Action)  — 목표 또는 세부목표에 연결되는 별도 축. 계획이행 영역에만 존재.
```

### 계층 명칭도 데이터다

화면에 나오는 「모니터링 영역」「목표」「세부목표」「지표」라는 **단어는 프로그램에 박혀 있지 않습니다.**
설정 화면(`/admin/config`)의 `level0_label`~`level3_label` 값을 바꾸면 공개 화면과 관리자 화면의
용어가 **한 번에 전부** 바뀝니다.

| 설정 키 | 현재 값 | 바꾼다면 예시 |
|---|---|---|
| `level0_label` | 모니터링 영역 | 분야 |
| `level1_label` | 목표 | 부문 |
| `level2_label` | 세부목표 | 추진전략 |
| `level3_label` | 지표 | 계획지표 |

실제로 개발 중 검증 단계에서 「부문 / 추진전략 / 계획지표」로 바꿔 화면 전체 용어가 일괄 변경되는 것을
확인했습니다. 이것이 **자체 계획지표 전환 리허설**이었습니다.

---

## 7. 공개 화면 기능 상세

### 7.1 `/` — 통합 현황

영역 3개를 **한 화면에서 나란히 비교**하는 첫 페이지입니다.

| 구역 | 내용 |
|---|---|
| 상단 히어로 | 영역별 색이 이어진 띠, 지표 체계 이름, 사이트 제목·부제, **전체 평균 달성도(%)**, 전체 지표 수 |
| 전체 이행 현황 | 6가지 상태별 지표 개수 카드 (달성/순조/지연/악화/모니터링/자료없음) |
| 영역별 현황 카드 | 영역마다 카드 1장 — 그라디언트 헤더(영역색), 아이콘, 설명, **평균 달성도 도넛**, 목표·지표·이행과제 개수, **상태 분포 막대**(상태별 색 비율), 소속 목표 목록과 지표 수 |
| 관리가 필요한 지표 | 전 영역에서 **지연·악화** 상태인 지표를 진행률 낮은 순 최대 8개. 클릭하면 해당 영역의 지표 상세가 바로 열림 |

영역 카드는 고정 3칸이 아니라 `auto-fit` 그리드라, 영역이 2개든 5개든 **한 줄을 꽉 채워** 배치됩니다.

### 7.2 `/[track]` — 영역 개요

| 구역 | 내용 |
|---|---|
| 히어로 | 영역명·설명, **평균 달성도**, 「지표 전체 보기」 버튼 |
| 이행 현황 | 해당 영역 지표의 상태별 집계 카드 |
| 목표별 현황 | 목표마다 카드 — 목표색 헤더, 도넛(평균 달성도), 지표 수, 상태 분포. 클릭하면 **그 목표의 지표만** 필터된 목록으로 이동 |
| 관리가 필요한 지표 | 지연·악화 상위 5개 |
| 목표에 근접한 지표 | 진행률 높은 순 5개 |
| 이행과제 요약 | (과제가 있는 영역만) 상태별 개수 |

### 7.3 `/[track]/indicators` — 지표 탐색

이 사이트에서 가장 많이 쓰게 될 화면입니다.

**필터·정렬**

| 기능 | 동작 |
|---|---|
| 목표 칩 | 「전체」 + 목표별 칩. 목표색이 칩에 그대로 반영되고 각 칩에 지표 수 표시 |
| 상태 칩 | 6가지 상태 배지를 눌러 필터. 한 번 더 누르면 해제. 해당 영역에 0건인 상태는 칩이 아예 안 나옴 |
| 검색 | 지표번호·지표명·정의·세부목표명·목표명·담당·출처를 한꺼번에 훑는 통합 검색 |
| 정렬 | 번호순(기본) / 달성률 높은 순 / 낮은 순 / 최신 데이터 연도순 |
| 보기 전환 | **카드 격자** ↔ **목록(표)** |
| 그룹 헤더 | 번호순 정렬일 때만 세부목표별로 묶어 헤더 표시 |

**지표 카드에 담긴 것**: 목표색 액센트 바, 지표번호·지표명, 상태 배지, 최신값과 단위,
전년 대비 증감(개선 방향이면 초록 화살표·악화면 빨강 — `direction` 설정에 따라 판단),
진행률 막대와 %, 기준값 → 목표값, **미니 시계열 스파크라인**, 자료가 3년 넘게 안 바뀌었으면
「자료 갱신 필요」 배지.

**상세 드로어**(카드 클릭 시 오른쪽에서 슬라이드): 큰 시계열 차트(기준선·목표선 포함),
정의·산출식·단위·방향, 기준연도/기준값, 목표연도/목표값, 출처(링크), 갱신주기, 담당,
비고, 상태 판정 근거 설명, 그리고 **이 지표의 원자료만 CSV로 내려받기** 버튼.

**딥링크**: `/plan/indicators?goal=<목표id>` 로 들어오면 그 목표만 필터된 상태로,
`?indicator=<지표id>` 로 들어오면 **상세 드로어가 자동으로 열린 채로** 시작합니다.
첫 화면의 「관리가 필요한 지표」 목록이 이 링크를 씁니다.

### 7.4 `/[track]/actions` — 이행과제 트래커

지표(숫자)와 별개로, **무엇을 하고 있는지**를 추적하는 화면입니다.

- 상태 4종: **완료 / 추진중 / 지연 / 예정** (지표의 6상태와는 별개 체계)
- 상단에 상태별 집계, 상태·목표·기한연도 필터, 통합 검색
- 목표별 아코디언으로 묶여 있고, 과제를 펼치면 요약문 + **근거 링크**(제목|URL 형식으로 여러 개)
- 담당 부서, 기한 연도, 최종 업데이트일 표시
- **이행과제가 0건인 영역에서는 이 탭이 상단 메뉴에 아예 나타나지 않습니다.**
  (환경상태·환경체감처럼 관측·조사만 하는 영역에 빈 트래커를 남겨두지 않기 위한 의도적 설계)

### 7.5 `/[track]/data` — 데이터·방법론

- **달성도 계산식 전문**과 왜 그렇게 계산하는지 설명 (9장 내용이 화면에 그대로 있음)
- 6가지 상태의 판정 기준 표
- 해당 영역 **지표 메타데이터 전체 표** — 번호·지표명·정의·단위·기준(연도)·최신(연도)·목표(연도)·
  출처(링크)·갱신주기·담당·상태
- 「전체 CSV 내려받기」 — 해당 영역 지표만 골라서 내려받습니다 (`/api/export?type=indicators&track=plan`)

---

## 8. 관리자 화면 기능 상세

`/admin` 이하 모든 화면은 **로그인해야** 보입니다. 로그인하지 않고 접근하면 `/login`으로 넘어갑니다.

### 8.1 `/admin` — 개요

| 구역 | 내용 |
|---|---|
| 데이터 규모 | 영역 / 목표·세부목표 / 지표 / 실적값 / 이행과제 개수 카드 (클릭하면 해당 관리 화면으로) |
| 이행 현황 | 상태별 집계 — **임시저장 항목 포함** |
| 점검이 필요한 항목 | ① 임시저장 상태 ② 실적값이 하나도 없는 지표 ③ 자료가 3년 넘게 안 바뀐 지표 ④ 목표값이 없는 지표 — 각각 목록으로 바로 이동 |
| 최근 수정 이력 | 최근 8건 |

### 8.2 `/admin/structure` — 계층 관리

**영역 → 목표 → 세부목표** 3단을 아코디언에서 통째로 관리합니다.

- 각 단계마다 추가 / 수정 / 삭제
- **삭제는 확인창**에서 "이 목표를 지우면 하위 세부목표 n개, 지표 m개가 함께 사라집니다"까지 알려줌
- 세부목표 추가 시 **다음 코드 자동 제안** (13-1, 13-2가 있으면 13-3을 미리 채워줌)
- 목표 편집에서 **상위 영역을 바꾸면 다른 영역으로 이동**
- 색은 비워두면 「색 자동배정」 — 팔레트에서 순서대로 배정
- 영역 코드는 주소로 쓰이므로 **영문 소문자·숫자·하이픈만** 허용 (검증됨)

### 8.3 `/admin/indicators` — 지표 목록

- 영역 / 목표 / 세부목표 / 상태 / 공개여부 필터 + 검색
- 표에서 지표번호·이름·단위·기준·최신·목표·진행률·상태 확인
- **눈 아이콘 클릭으로 공개 ↔ 임시저장 즉시 전환** (별도 저장 불필요)
- 「지표 추가」 버튼 → `/admin/indicators/new`

### 8.4 `/admin/indicators/[id]` — 지표 편집

한 화면에서 두 가지를 합니다.

**① 메타데이터 편집** — 지표번호, 소속 세부목표, 지표명, 정의, 산출식, 단위,
방향(값이 클수록 좋음 / 작을수록 좋음), 기준연도·기준값, 목표연도·목표값, 출처·출처URL,
갱신주기, 담당, 비고, 대표지표 여부, 공개 여부, 정렬 순서, **상태 직접 지정**(자동 판정을 덮어씀).

**② 연도별 실적값 인라인 편집** — 표 안에서 바로 행 추가·수정·삭제.
연도는 마지막 값 다음 해를 미리 채워주고, 지역(기본 「전국」)과 비고(「잠정치」 등)도 넣을 수 있습니다.
값을 하나 추가하면 **최신값·진행률·상태·차트가 즉시 다시 계산**됩니다.
화면 위쪽에 계산 결과 미리보기가 함께 나옵니다.

> **값이 없으면 빈 칸으로 두세요.** `0`은 "실제로 0"이라는 뜻입니다.

### 8.5 `/admin/actions` — 이행과제 관리

과제번호, 소속 목표/세부목표, 제목, 요약, 상태(완료/추진중/지연/예정), 기한 연도, 담당,
최종 업데이트일, 근거 링크(`제목|URL;제목|URL` 형식으로 여러 개), 공개 여부를 CRUD.

### 8.6 `/admin/data` — CSV 가져오기·내보내기

- **내려받기**: 7종(영역/목표/세부목표/지표/실적값/이행과제/설정) 각각 버튼 하나. 현재 건수도 표시.
- **올리기**: 파일을 고르거나, **엑셀·구글시트에서 표를 복사해 그대로 붙여넣기**(탭 구분 자동 인식).
- 「기존 데이터를 모두 지우고 넣기」 옵션 별도 제공(기본은 덮어쓰기+추가).
- 처음부터 전체를 넣을 때의 **업로드 순서 안내**(영역 → 목표 → 세부목표 → 지표 → 실적값 → 이행과제 → 설정)
  — 상위 항목이 먼저 있어야 참조가 연결되기 때문입니다.
- 각 종류의 **컬럼 형식 안내표**가 화면 아래에 함께 있습니다.

### 8.7 `/admin/config` — 사이트 설정

사이트 제목, 부제, **계층 명칭 4종**, 지표 체계 이름, 기관명, 문의처, 최종 갱신일, 푸터 안내문구를
편집합니다. 항목마다 "이걸 바꾸면 어디가 바뀌는지" 설명이 붙어 있고, **새 설정 키를 직접 추가**할 수도
있습니다.

### 8.8 `/admin/logs` — 수정 이력

최근 300건. 시각 / 작업 종류(추가·수정·삭제·가져오기) / 대상 종류 / 대상 이름 / **변경 상세(JSON)** 를 표로 봅니다.
계정 개념이 없는 대신 이 이력이 추적성을 담당합니다.

### 8.9 `/admin/preview` — 미리보기

공개 화면과 **똑같은 부품으로** 렌더하되 **임시저장 항목까지 포함**해서 보여줍니다.
임시저장 지표에는 「임시저장」 배지가 붙습니다. 공개 전에 최종 확인하는 용도입니다.

---

## 9. 달성도 계산 규칙 (이 사이트의 심장)

계산 규칙은 **`src/lib/progress.ts` 파일 한 곳**에만 있습니다. 화면 어디서 보든 같은 숫자가 나오는 이유입니다.

### 9.1 두 개의 공식

```
진행률(%)     = (최신값 − 기준값) ÷ (목표값 − 기준값) × 100
기대진행률(%) = (최신 데이터 연도 − 기준연도) ÷ (목표연도 − 기준연도) × 100
```

**① 왜 「값이 작아져야 좋은 지표」도 같은 식으로 되나**
분모 `(목표값 − 기준값)`의 **부호가 방향을 자동으로 흡수**하기 때문입니다.
온실가스 배출량처럼 줄어야 좋은 지표는 분자·분모가 모두 음수가 되어 결과가 양수로 나옵니다.
그래서 `direction`(up/down) 설정은 **계산에 쓰이지 않고**, 증감 화살표 색과 안내 문구에만 씁니다.

**② 왜 기대진행률이 「현재연도」가 아니라 「최신 데이터 연도」 기준인가**
환경 통계는 공표가 1~2년 늦습니다. 2026년 시점에 최신 자료가 2024년인 경우가 흔합니다.
현재연도(2026) 기대치와 비교하면 **정상적으로 진행 중인 지표까지 전부 「지연」으로 찍힙니다.**
그래서 "그 자료가 나온 시점에 기대되던 수준"과 비교합니다. **이 규칙은 바꾸면 안 됩니다.**

### 9.2 상태 6종 판정 기준

| 상태 | 판정 조건 | 색 | 의미 |
|---|---|---|---|
| **달성** | 진행률 ≥ 100% | 초록 `#2B8A3E` | 목표값에 도달 |
| **순조** | 진행률 ≥ 기대진행률 × 0.9 | 파랑 `#1971C2` | 기대 속도를 따라가는 중 |
| **지연** | 진행률 < 기대진행률 × 0.9 | 주황 `#E8590C` | 기대 속도에 못 미침 |
| **악화** | 진행률 < 0 | 빨강 `#C92A2A` | 기준값보다 후퇴 |
| **모니터링** | 목표값이 설정되지 않음 | 회색 `#868E96` | 관찰만 하는 지표 |
| **자료없음** | 실적값이 하나도 없음 | 연회색 `#ADB5BD` | 아직 입력 전 |

부가 규칙:
- 최신 자료가 **3년**을 넘겨 갱신되지 않으면 「자료 갱신 필요」 배지가 함께 붙습니다.
- 관리자가 상태를 직접 지정하면 자동 판정 대신 그 값을 씁니다(예외 처리용).
- 화면에 표시되는 진행률은 0~100%로 자릅니다. 초과 달성이나 음수는 내부적으로만 유지합니다.
- **평균 달성도**를 낼 때 「자료없음」·「모니터링」 지표는 제외합니다.

### 9.3 실제 데이터로 계산해 보기

**예시 ① 값이 커져야 좋은 지표 — 「방재시설 집행 비율」(13-1-1, 단위 %)**

```
기준: 2020년 72.4     목표: 2030년 95.0     최신: 2024년 88.1

진행률     = (88.1 − 72.4) ÷ (95.0 − 72.4) × 100 = 15.7 ÷ 22.6 × 100 ≈ 69.5%
기대진행률 = (2024 − 2020) ÷ (2030 − 2020) × 100 = 4 ÷ 10 × 100     = 40.0%

69.5% ≥ 40.0% × 0.9(=36.0%)  →  상태 「순조」
```

**예시 ② 값이 작아져야 좋은 지표 — 「국가 온실가스 총배출량」(13-4-1, 백만 tCO2eq)**

```
기준: 2018년 727.6    목표: 2030년 436.6    최신: 2023년 624.0

진행률     = (624.0 − 727.6) ÷ (436.6 − 727.6) × 100
           = (−103.6) ÷ (−291.0) × 100 ≈ 35.6%      ← 음수 ÷ 음수 = 양수
기대진행률 = (2023 − 2018) ÷ (2030 − 2018) × 100 = 5 ÷ 12 × 100 ≈ 41.7%

35.6% < 41.7% × 0.9(=37.5%)  →  상태 「지연」
```

두 지표 모두 **똑같은 공식 한 줄**로 계산됩니다. 방향을 따로 분기하지 않습니다.

---

## 10. 색상 체계

색이 두 층으로 분리되어 있습니다. 이유는 "**다양성**은 목표별로, **일관성**은 상태별로"입니다.

### 10.1 목표색 — 다양성

목표(또는 영역)마다 색을 **하나만** 지정하면, 프로그램이 그 색을 HSL로 분해해
**배경 tint / 카드 보더 / 텍스트색 / 그라디언트 끝색 / 반투명 3단계**를 자동으로 파생시킵니다.
그리고 이 톤이 헤더 그라디언트 · 카드 좌측 액센트 바 · 도넛 · 차트 선 · 필터 칩까지 전파됩니다.
목표 섹션마다 화면 인상이 확 달라지는 이유입니다.

**색을 지정하지 않으면** 10색 팔레트에서 순서대로 자동 배정됩니다.
목표를 4개, 6개로 늘려도 색이 겹치거나 비어 보이지 않습니다.

### 10.2 상태색 — 일관성

달성/순조/지연/악화/모니터링/자료없음 6색은 **전 화면에서 동일한 의미로만** 쓰이고 변주하지 않습니다.
색맹 대비로 **색 + 아이콘 + 텍스트**를 함께 표기합니다.

---

## 11. 데이터베이스 구조

파일 하나(`prisma/dev.db`)에 표 8개가 들어 있습니다. 설계도는 `prisma/schema.prisma`입니다.

| 표(테이블) | 담는 것 | 주요 칸 |
|---|---|---|
| `Track` | 모니터링 영역 | 코드(주소로 사용, 고유), 이름, 설명, 색, 아이콘, 정렬순서, 공개여부 |
| `Goal` | 목표 | 코드(고유), 소속 영역, 표시번호, 이름, 설명, 색, 아이콘, 순서, 공개여부 |
| `Target` | 세부목표 | 코드(고유), 소속 목표, 이름, 설명, 순서, 공개여부 |
| `Indicator` | 지표 | 코드(고유), 소속 세부목표, 이름, 정의, 산출식, 단위, 방향, 기준연도·기준값, 목표연도·목표값, 출처·URL, 갱신주기, 담당, 상태 직접지정, 대표지표, 공개여부, 비고, 순서 |
| `IndicatorValue` | 연도별 실적값 | 소속 지표, 연도, 값, 지역(기본 「전국」), 비고 — **(지표+연도+지역) 조합이 고유** |
| `Action` | 이행과제 | 코드(고유), 소속 목표/세부목표, 제목, 요약, 상태, 기한연도, 담당, 최종수정일, 링크, 순서, 공개여부 |
| `Config` | 사이트 설정 | 키, 값 |
| `AuditLog` | 수정 이력 | 시각, 작업, 대상 종류, 대상 id, 대상 이름, 변경 상세(JSON) |

**연쇄 삭제 규칙**: 영역을 지우면 그 아래 목표·세부목표·지표·실적값이 함께 사라집니다.
다만 이행과제는 목표가 사라져도 **과제 자체는 남고 연결만 끊깁니다**(자료 보존 목적).

**임시저장 플래그**: 영역·목표·세부목표·지표·이행과제 모두 `published`(공개여부)를 가집니다.
`false`면 공개 화면에서 감춰지지만 데이터는 그대로 보존되고, `/admin/preview`에서 확인할 수 있습니다.

**데이터 조회 방식**: 화면을 그릴 때 `getDashboard()` 함수 하나가 영역→목표→세부목표→지표→실적값을
**한 번에** 읽고, 각 지표에 계산 결과와 상위 계층 정보(목표명·색·영역코드 등)를 **평탄화해서 붙여** 내려줍니다.
필터나 카드에서 매번 관계를 되짚지 않아도 되게 하려는 의도입니다.

---

## 12. CSV 왕복 (엑셀과 주고받기)

이 기능의 원칙은 하나입니다 — **내보낸 파일을 그대로 다시 올릴 수 있어야 한다.**

| 종류 | 파일명 | 컬럼 |
|---|---|---|
| 영역 | `tracks.csv` | track_id, track_name, track_desc, color, icon, order, display |
| 목표 | `goals.csv` | goal_id, track_id, goal_no, goal_name, goal_desc, color, icon, order, display |
| 세부목표 | `targets.csv` | target_id, goal_id, target_name, target_desc, order, display |
| 지표 | `indicators.csv` | indicator_id, target_id, indicator_name, definition, method, unit, direction, baseline_year, baseline_value, target_year, target_value, source, source_url, update_cycle, custodian, status_override, is_headline, display, note |
| 실적값 | `values.csv` | indicator_id, year, value, region, note |
| 이행과제 | `actions.csv` | action_id, goal_id, target_id, title, summary, status, due_year, responsible, last_update, links |
| 설정 | `config.csv` | key, value |

**가져오기 동작**
- 고유번호(`indicator_id` 등) 기준 **upsert** — 있으면 덮어쓰고 없으면 새로 만듭니다.
- **탭 구분(TSV) 자동 감지** — 엑셀·구글시트에서 표를 복사해 붙여넣으면 그대로 들어갑니다.
- **행 단위 오류 처리** — 한 줄이 잘못돼도 전체가 중단되지 않고, "몇 행에서 왜 실패했는지"를 결과 메시지로 알려줍니다.
- 결과는 「n건 추가, m건 수정, k건 건너뜀」 형태로 표시되고 이력에도 남습니다.

**내보내기 동작**
- 엑셀에서 한글이 깨지지 않도록 **BOM**을 붙여 내려줍니다.
- 주소에 `?track=plan` 을 붙이면 해당 영역 것만 필터해서 받습니다.

`sheets/` 폴더의 CSV들은 **초기 시드 데이터이자 형식 견본**입니다. `npm run db:seed`가 이 파일들을 읽어
DB를 채웁니다(기존 데이터를 전부 지우고 다시 넣습니다).

---

## 13. 로그인·보안·수정 이력

| 항목 | 방식 | 이유 |
|---|---|---|
| 로그인 | **공용 비밀번호 1개** (`.env`의 `ADMIN_PASSWORD`) | 팀 규모가 작아 계정 관리 비용이 이득보다 큼 |
| 세션 유지 | HMAC-SHA256으로 서명한 **httpOnly 쿠키**, 유효 12시간 | 자바스크립트로 훔칠 수 없고, 위조하면 서명 검증에서 걸림 |
| 비밀번호 비교 | `timingSafeEqual` (일정 시간 비교) | 응답 시간 차이로 비밀번호를 추측하는 공격 차단 |
| 화면 보호 | `/admin` 레이아웃에서 인증 확인 → 미인증이면 `/login` |  |
| **쓰기 보호** | 저장·삭제 기능 **각각**이 스스로 인증을 다시 확인 | 화면 보호만으로는 부족 — 주소를 직접 두드리는 요청을 막기 위해 |
| 추적성 | 모든 쓰기가 **AuditLog에 자동 기록** | 계정이 없는 대신 "무엇이 언제 바뀌었나"를 남김 |

> 배포 전에 `.env`의 `ADMIN_PASSWORD`와 `SESSION_SECRET`을 **반드시** 바꾸세요.
> `SESSION_SECRET`은 `openssl rand -hex 32` 같은 명령으로 만든 긴 랜덤 문자열이 좋습니다.

---

## 14. Claude Code로 어떻게 만들었나

이 장은 **개발 과정 자체**를 기록한 것입니다. "AI로 만들었다"가 구체적으로 무엇을 뜻하는지,
사람은 무엇을 했고 AI는 무엇을 했는지, 어떻게 품질을 확인했는지를 남깁니다.

### 14.1 Claude Code란

**Claude Code**는 터미널(검은 명령창)에서 대화하듯 지시하면, AI가 **저장소의 파일을 직접 읽고 · 코드를
쓰고 · 명령을 실행하고 · 결과를 확인**하는 개발 도구입니다. 챗봇에 코드를 물어보고 복사·붙여넣기 하는
방식과 다른 점은 세 가지입니다.

| | 일반 챗봇 | Claude Code |
|---|---|---|
| 파일 | 사람이 복사해서 보여줘야 함 | 저장소를 직접 열어 읽음 |
| 수정 | 사람이 붙여넣기 | 파일을 직접 수정 |
| 검증 | 사람이 실행해보고 결과를 다시 알려줌 | 빌드·타입검사·서버 실행·HTTP 요청까지 직접 하고 결과를 봄 |

즉 "코드를 알려주는 도구"가 아니라 **"작업을 수행하는 도구"** 입니다.

### 14.2 역할 분담 — 사람이 한 일 / AI가 한 일

| 사람(발주·검수) | Claude Code(설계·구현) |
|---|---|
| 무엇을 왜 만들지 정의 (환경계획 이행을 한눈에) | 그 요구를 화면·데이터 구조로 번역 |
| 참고 모델 지정 (EU JRC 두 사이트) | 참고 요소를 분해해 기능 목록으로 정리 |
| 방향 전환 결정 (구글시트 → 자체 DB) | 전환에 필요한 스키마·마이그레이션·화면 전부 구현 |
| "영역을 3개로 나누자" 같은 구조 변경 요구 | 최소 변경으로 구조를 확장하는 방법 설계 후 구현 |
| 결과 확인, 수용 여부 판단 | 빌드·타입체크·수용기준 검증 수행 및 보고 |
| 공개 여부·수치 검수 책임 | 검수가 필요하다는 경고를 문서·화면에 명시 |

**의사결정은 사람이, 구현과 그 근거 제시는 AI가** 하는 구조입니다.

### 14.3 실제 진행 순서

#### ① 문제 정의와 참고 모델 조사
연 1회 보고서 방식의 세 가지 문제(열람성·갱신성·추적성)를 정리하고, EU JRC의 Actions Tracker와
BDS 2030 Dashboard에서 가져올 요소를 각각 목록화했습니다. **두 사이트를 한 앱에 합친다**는 것이
이 단계의 결론입니다.

#### ② 1차 설계 — 구글 스프레드시트 기반 (그리고 폐기)
처음 설계는 "**서버 없이** 구글시트를 원천으로 두고 CSV를 직접 읽어오는" 방식이었습니다.
데이터가 적고 담당자 몇 명만 수정하니 그게 가볍다고 봤고, 실제로 **시트 탭 6개 구조와 시드 CSV까지
전부 만들었습니다.**

그런데 검토 결과 **탭 6개를 사람이 직접 관리하는 게 오히려 번거롭고 실수하기 쉽다**는 판단이 나왔습니다.
(어느 탭에 뭘 넣는지 헷갈림 / 컬럼 규칙 위반 / 시트를 외부에 공개해야 하는 제약)

#### ③ 방향 전환 — Next.js + SQLite + 관리자 CRUD
관리자 화면이 있는 자체 앱으로 전환했습니다. 다만 **이미 만든 CSV를 버리지 않았습니다.**
초기 시드 데이터이자 **CSV 가져오기 형식의 견본**으로 계속 씁니다.
덕분에 "엑셀로 대량 편집"이라는 시트 방식의 장점도 `/admin/data`에 그대로 남아 있습니다.

> 이 결정 기록은 `PLAN.md`에 남아 있습니다. **폐기된 설계와 그 이유를 남기는 것**이
> 나중에 같은 논의를 반복하지 않게 해줍니다.

#### ④ 뼈대 구축
Next.js 앱 생성 → `prisma/schema.prisma`에 표 구조 정의 → `npm run db:push`로 DB 생성 →
`prisma/seed.ts`로 `sheets/*.csv`를 읽어 초기 데이터 주입.

#### ⑤ 계산 규칙을 한 파일에 격리
`src/lib/progress.ts` 하나에 진행률·기대진행률·상태 6종 판정·평균·집계를 모두 넣었습니다.
**화면이 늘어나도 계산이 흩어지지 않게** 하는 것이 목적입니다.

#### ⑥ 공개 화면 → 관리자 화면
공개 4화면을 먼저 만들고, 이어서 관리자 10화면을 만들었습니다.
쓰기는 전부 **서버 액션** 한 갈래로 모으고, 각 액션이 ① 인증 확인 → ② 작업 → ③ **이력 기록** →
④ 화면 갱신 → ⑤ 사람이 읽을 수 있는 결과 메시지 반환을 같은 순서로 하도록 패턴을 고정했습니다.
(예: DB의 중복 오류 → "이미 같은 번호가 존재합니다"로 번역)

#### ⑦ 검증에서 설계 오류를 발견하고 고침 ★
수용 기준 20개 항목을 하나씩 확인하던 중 **가장 중요한 버그**를 잡았습니다.

> 기대진행률을 **현재연도** 기준으로 계산하고 있었는데, 통계 공표가 1~2년 늦는 지표는
> 정상 진행 중인데도 전부 「지연」으로 찍혔습니다. → **최신 데이터 연도 기준**으로 변경.

이 수정 하나로 샘플 13개 지표의 판정이 **「지연 10건」 → 「순조 5 / 지연 5 / 달성 1 / 악화 1 / 모니터링 1」**
로 바뀌었습니다. 화면을 실제로 돌려보고 숫자를 눈으로 확인하지 않았다면 그대로 배포됐을 오류입니다.

#### ⑧ 1차 완성 커밋
```
60941ca  K-SDGs 환경분야 지표 대시보드 초기 버전   (107개 파일, 19,652줄 추가)
```

#### ⑨ 규칙 문서(CLAUDE.md) 작성
```
88707d1  CLAUDE.md 추가
```
"향후 세션이 **모르면 되돌리기 쉬운 결정**을 명시" — 이것이 이 커밋의 목적입니다. (14.4에서 상술)

#### ⑩ 2차 확장 — 모니터링 영역 3분할
"상단 메뉴를 계획이행 / 환경상태 / 환경체감 3개로 나누자"는 요구가 들어왔습니다.
이걸 **새 기능 3개를 만드는 대신 계층의 최상위 축 하나를 얹는 것**으로 해결했습니다.

- `Track` 표를 하나 추가하고 `Goal`에 소속 영역을 붙임 — 그 아래 구조와 계산은 **완전히 동일**
- 주소를 코드에 박지 않고 `[track]` **동적 세그먼트**로 처리 → 영역을 추가하면 메뉴·주소가 자동 생성
- 이행과제가 0건인 영역은 탭 자체를 숨김
- 환경상태 10개 · 환경체감 9개 지표를 추가하되, **영역마다 6가지 상태가 고루 나오도록** 값을 구성
- CSV를 7종으로 확대하고 영역별 필터 내려받기 추가

```
60a1085  모니터링 영역 3분할 — 계획이행·환경상태·환경체감   (회귀 29항목 통과)
```

#### ⑪ 3차 — K-SDGs → EP 브랜딩 교체 (2026-08-19)
"프로그램에 K-SDGs 표기가 남아 있으니 EP(Environmental Planning)로 바꿔달라"는 요구를 처리했습니다.
`grep`으로 저장소 전체를 훑어 실제 표기가 있는 곳을 전부 찾아낸 뒤 여섯 군데를 고쳤습니다.

| 위치 | 이전 | 이후 |
|---|---|---|
| 브라우저 탭 제목·설명 (`src/app/layout.tsx`) | K-SDGs 환경분야 지표 대시보드 | 환경계획(EP) 통합 모니터링 대시보드 |
| 지표 체계 이름 (`sheets/config.csv` + DB) | 국가 지속가능발전목표(K-SDGs) 기반 시범 구성 | 환경계획(EP, Environmental Planning) 기반 시범 구성 |
| 로그인 쿠키 이름 (`src/lib/auth.ts`) | `ksdgs_session` | `ep_session` |
| 패키지 이름 (`package.json`) | `ksdgs-dashboard` | `ep-dashboard` |
| DB 설계도 주석 (`prisma/schema.prisma`) | K-SDGs … 데이터 모델 | 환경계획(EP) … 데이터 모델 |
| 구버전 시트 (`sheets/config.tsv`) | K-SDGs 표기 | EP 표기 |

지표 체계 이름은 DB에 이미 저장된 값이라, 시드를 다시 붓지 않고 **해당 한 줄만 갱신**해서
기존 지표·실적값·이행과제를 그대로 보존했습니다. 이후 타입 검사와 6개 화면 응답을 확인했습니다.

> **주의**: 쿠키 이름이 바뀌었으므로 기존 로그인 세션은 무효가 됩니다. 다시 로그인하면 됩니다.
> 브라우저 탭 제목이 여전히 예전 이름으로 보이면 **강력 새로고침(Ctrl+Shift+R)** 하세요.

### 14.4 `CLAUDE.md` — AI에게 주는 저장소 규칙서

Claude Code는 이 저장소에서 작업을 시작할 때 **`CLAUDE.md`를 자동으로 읽습니다.**
사람으로 치면 "신입에게 주는 온보딩 문서 + 하지 말아야 할 것 목록"입니다.
여기에 적힌 규칙은 **다음 세션, 다음 사람에게도 그대로 적용**됩니다.

여기에 무엇을 적었는지가 이 프로젝트의 핵심입니다.

| 적어둔 규칙 | 왜 적었나 |
|---|---|
| **코드에 K-SDGs를 하드코딩하지 않는다** — 개수 상수, `13`·`G13`·`"state"` 같은 값, 「목표」「지표」라는 단어를 화면에 직접 쓰는 것 모두 금지 | 모르면 무심코 되돌아가는 결정. 이게 깨지면 계획지표 전환이 "코드 수정"이 되어버림 |
| **기대진행률은 최신 데이터 연도 기준** — 이 규칙을 바꾸지 말 것 | 현재연도가 자연스러워 보여서 되돌리기 쉬운데, 되돌리면 전부 「지연」이 됨 |
| 그리드는 고정 열이 아니라 `auto-fit` | 항목이 몇 개든 화면이 깨지지 않게 |
| 상태색은 변주 금지 | 같은 색이 화면마다 다른 뜻이 되면 안 됨 |
| **dev 서버를 켠 채 `npm run build` 하지 말 것** | 둘이 같은 폴더를 써서 실행 중인 서버가 깨짐 (`rm -rf .next` 후 재시작해야 함) |
| Node 18에서는 `npm run fix:node18` 한 번 실행 | Tailwind 네이티브 모듈이 조용히 빠져 빌드가 실패 |
| 서버 액션 추가 시 지킬 5단계 패턴 | 인증·이력 기록이 빠진 액션이 생기지 않게 |
| CSV 내보내기/가져오기는 **한쪽만 바꾸면 왕복이 깨진다** | 두 파일이 같은 컬럼명을 공유 |
| "값 없음은 빈 칸, `0`은 실제로 0" | 데이터 해석 사고 방지 |
| 예시 수치 경고 문구를 제거하지 말 것 | 검수 전 대외 공개 사고 방지 |

**기능을 추가할 때마다 "목표가 4개, 6개로 늘어도 그대로 동작하는가"를 확인할 것** —
이 한 문장이 프레임워크 중립성을 지키는 실질적인 체크리스트입니다.

### 14.5 문서 5종 체계

코드만 남기지 않고, **역할이 다른 문서를 나눠서** 남겼습니다.

| 문서 | 답하는 질문 | 주 독자 |
|---|---|---|
| `README.md` | 어떻게 실행하나? 화면이 뭐가 있나? | 새로 합류한 개발자 |
| `PRD.md` | 무엇을 왜 만들었나? 무엇을 만족해야 하나? | 기획·발주·의사결정자 |
| `REQUIREMENTS.md` | 처음부터 다시 만든다면 정확히 무엇이 필요한가? (16장, 함정 목록 포함) | 재구축·인수인계 |
| `PLAN.md` | 왜 이렇게 설계했나? 폐기된 안은 무엇이고 왜 폐기했나? | 설계 변경을 검토할 사람 |
| `CLAUDE.md` | 이 저장소에서 작업할 때 지켜야 할 규칙은? | **AI 세션 + 개발자** |
| `OVERVIEW.md` (이 문서) | 처음 보는 사람에게 전부 설명 | 비개발자·이해관계자 |

> 코드는 "어떻게"만 말합니다. **"왜"는 사라지기 쉬워서 따로 남겨야 합니다.**
> AI와 함께 개발할 때는 특히 중요합니다 — 다음 세션의 AI는 코드는 읽을 수 있어도
> 지난번 회의에서 왜 그렇게 정했는지는 문서에서만 알 수 있기 때문입니다.

### 14.6 자동화 테스트가 없는데 어떻게 검증했나

이 프로젝트에는 **자동 테스트 코드가 없습니다.** 대신 다음 방식으로 검증했습니다.

1. **빌드 + 타입 검사** — `npm run build`, `npx tsc --noEmit` 통과 확인
2. **실제 서버를 띄우고 화면을 요청** — 공개 4화면 + 관리자 10화면이 정상 응답하는지 확인
3. **폼 제출을 그대로 재현** — 렌더된 HTML에서 폼의 숨은 식별자(`$ACTION_*`)를 뽑아내 실제 저장 요청을
   그대로 다시 보내는 방식. 즉 **사람이 버튼을 누른 것과 동일한 요청**을 만들어 보냅니다.
4. **결과를 DB와 화면 양쪽에서 확인** — 저장이 됐는지, 계산이 다시 됐는지, 공개 화면에 반영됐는지
5. **검증에 쓴 데이터는 원래대로 되돌림**

수용 기준(1차 20항목 / 2차 회귀 29항목)은 `REQUIREMENTS.md` 14장에 목록으로 있습니다. 그중 몇 가지:

- 틀린 비밀번호는 거부되고 쿠키가 발급되지 않는다
- 로그인 없이 `/admin`에 접근하면 `/login`으로 넘어간다
- 실적값을 추가하면 최신값·달성률이 즉시 다시 계산된다
- **색을 지정하지 않은 목표를 4번째로 추가하면 카드가 자동으로 늘고 색이 자동 배정된다 (코드 수정 0줄)**
- 지표를 임시저장으로 바꾸면 공개 화면에서 사라지고 미리보기에는 배지와 함께 남는다
- 엑셀에서 복사한 탭 구분 표를 붙여넣으면 실적값이 일괄 반영된다
- **계층 명칭을 「부문 / 추진전략 / 계획지표」로 바꾸면 화면 전체 용어가 한 번에 바뀐다** (전환 리허설)

### 14.7 커밋 기록

| 커밋 | 날짜 | 내용 |
|---|---|---|
| `60941ca` | 2026-08-12 00:09 | 초기 버전 — 공개 4화면 + 관리자 10화면, 107개 파일 |
| `88707d1` | 2026-08-12 00:11 | `CLAUDE.md` 추가 — 저장소 규칙 명문화 |
| `60a1085` | 2026-08-12 00:43 | 모니터링 영역 3분할 — `Track` 도입, 동적 라우팅, 지표 19개 추가 |
| (작업 중) | 2026-08-19 | K-SDGs → EP 브랜딩 교체, 이 안내서 작성 |

커밋 메시지에 **`Co-Authored-By: Claude Opus 5`** 가 함께 기록되어 있어, 어느 작업에 AI가 참여했는지
이력에 남습니다.

### 14.8 이 방식으로 개발할 때의 교훈

**잘 통한 것**

- **규칙을 문서로 고정**(`CLAUDE.md`)하면 세션이 바뀌어도 설계 원칙이 유지된다.
- **"왜"를 남기면**(`PLAN.md`) 폐기된 설계를 다시 논의하지 않게 된다.
- 계산 규칙처럼 중요한 로직은 **한 파일에 격리**해두면 AI든 사람이든 실수로 흩뜨리기 어렵다.
- **실제로 실행해서 숫자를 눈으로 확인**하는 검증이, 코드 리뷰만으로는 못 잡는 설계 오류(기대진행률)를 잡았다.

**주의할 것**

- AI는 요구하지 않으면 "그럴듯한 기본값"을 넣는다. 이 프로젝트의 **예시 수치는 실제 공식 통계가 아니며**,
  대외 공개 전 소관 부서 검수가 반드시 필요합니다. (문서·화면·푸터에 경고를 남겨둔 이유)
- 하드코딩 금지 같은 제약은 **매번 명시**해야 유지된다. 그래서 규칙서에 적어둔 것입니다.
- 자동 테스트가 없으므로, 큰 변경 뒤에는 수용 기준 목록을 **다시 한 번 훑는 절차**가 필요합니다.

---

## 15. 기술 스택 (무엇으로 만들어졌나)

| 층 | 사용 기술 | 고른 이유 |
|---|---|---|
| 웹 프레임워크 | **Next.js 15** (App Router) + **React 19** | 화면과 서버 기능을 한 프로젝트에서. 서버 액션으로 별도 API 없이 폼 → DB 처리 |
| 언어 | **TypeScript 5** | 실행 전에 형 오류를 잡음 |
| 데이터베이스 | **SQLite** + **Prisma 6** | 파일 하나면 끝. 계정·서버 설정 불필요. 나중에 PostgreSQL 전환도 Prisma라 변경 최소 |
| 스타일 | **Tailwind CSS v4** | 클래스 조합으로 빠르게. 디자인 토큰을 CSS 변수로 관리 |
| UI 부품 | **shadcn/ui v4** (Base UI 기반) | 부품을 **소스로 저장소에 들고 있어** 자유롭게 수정 가능 |
| 차트 | **Recharts** | 시계열 그래프. 첫 로딩이 무거워지지 않도록 **지연 로딩** 처리 |
| 아이콘 | **lucide-react** | |
| 알림 | **sonner** | 저장 결과 토스트 메시지 |
| CSV 파싱 | **papaparse** | 따옴표·줄바꿈이 든 값도 안전하게 처리 |
| 폰트 | **Noto Sans KR** | 실제 쓰는 굵기 3종만 불러 폰트 트래픽을 1/4로 줄임 |

**실행 환경**: Node.js 20 이상 권장. (Node 18에서는 `npm run fix:node18`을 한 번 실행해야 빌드됩니다.)

---

## 16. 설치·실행

```bash
npm install          # 1. 필요한 라이브러리 설치
npm run db:push      # 2. DB 파일 생성 (prisma/dev.db)
npm run db:seed      # 3. sheets/*.csv 를 초기 데이터로 주입
npm run dev          # 4. 개발 서버 실행 → http://localhost:3000
```

포트를 바꾸려면 `PORT=8006 npm run dev` 처럼 앞에 붙입니다.

**운영 실행**

```bash
npm run build
npm run start
```

**관리자 로그인**: `http://localhost:3000/admin` → 비밀번호는 `.env`의 `ADMIN_PASSWORD` (기본값 `admin1234`).

**그 밖의 명령**

| 명령 | 하는 일 |
|---|---|
| `npm run lint` | 코드 스타일 검사 |
| `npx tsc --noEmit` | 타입 검사만 빠르게 |
| `npm run db:studio` | 브라우저에서 DB 표를 직접 열어봄 |
| `npm run db:seed` | **기존 데이터를 전부 지우고** CSV로 다시 채움 |

> ⚠️ **개발 서버를 켜둔 채로 `npm run build` 를 실행하지 마세요.** 둘이 같은 작업 폴더(`.next`)를 쓰기 때문에
> 실행 중인 서버가 깨집니다. 그렇게 됐다면 `rm -rf .next` 후 다시 시작하면 됩니다.

---

## 17. 자주 하는 작업 레시피

### 17.1 지표 하나 추가하기
`/admin/indicators` → 「지표 추가」 → 소속 세부목표 선택 → 지표번호·이름·단위·방향 입력 →
기준연도·기준값, 목표연도·목표값 입력 → 저장 → 편집 화면에서 **연도별 실적값 입력** → 「공개」 체크.

### 17.2 새 통계가 나와서 실적값만 갱신하기
`/admin/indicators`에서 지표 검색 → 편집 화면 → 실적값 표에서 **연도·값 입력 후 추가**.
최신값·진행률·상태·차트가 즉시 갱신됩니다. 「최종 갱신일」은 `/admin/config`에서 함께 바꿔주면 좋습니다.

### 17.3 여러 지표의 실적값을 엑셀로 한 번에 넣기
`/admin/data` → 「실적값」 선택 → 엑셀에서 `indicator_id, year, value, region, note` 4~5칸을
복사해서 붙여넣기 → 올리기. 결과 메시지에서 몇 건 추가/수정/건너뜀인지 확인.

### 17.4 모니터링 영역 추가하기
`/admin/structure` → 「모니터링 영역 추가」 → 주소 코드(영문 소문자·숫자·하이픈), 이름, 아이콘, 색 입력.
저장하면 **상단 메뉴와 `/새코드` 주소가 자동으로 생깁니다.** 코드는 한 줄도 고치지 않습니다.

### 17.5 자체 계획지표로 전환하기 (최종 목표)
1. `/admin/config`에서 계층 명칭을 계획 체계에 맞게 변경 (예: 부문 / 추진전략 / 계획지표)
2. 사이트 제목·부제·지표 체계 이름·기관명 변경
3. `/admin/data`에서 CSV로 새 계층·지표·실적값을 일괄 투입
   (순서: 영역 → 목표 → 세부목표 → 지표 → 실적값 → 이행과제)
4. 예시 데이터 삭제 또는 「기존 데이터를 모두 지우고 넣기」 옵션 사용
5. `/admin/preview`에서 확인 후 공개

**이 과정에 프로그램 수정은 없습니다.** 그것이 이 사이트의 설계 목적입니다.

### 17.6 임시로 감추기
지표 목록의 **눈 아이콘**을 누르면 즉시 임시저장(비공개)이 됩니다. 데이터는 남고 공개 화면에서만 사라집니다.

---

## 18. 주의사항과 한계

### 반드시 알아야 할 것

| 항목 | 내용 |
|---|---|
| **수치는 형식 예시입니다** | `sheets/*.csv`와 현재 DB의 값은 **형식 견본**이며 실제 공식 통계와 다릅니다. 대외 공개 전 소관 부서 검수·교체가 필요합니다. 화면 푸터·README에 있는 이 경고 문구를 **제거하지 마세요.** |
| **배포 전 비밀번호 교체** | `.env`의 `ADMIN_PASSWORD`, `SESSION_SECRET`을 반드시 바꾸세요. |
| **값 없음 ≠ 0** | 값이 없으면 빈 칸으로 두세요. `0`은 "실제로 0"이라는 뜻입니다. |
| **계정 구분이 없음** | 관리자는 공용 비밀번호 1개입니다. 누가 고쳤는지가 아니라 **무엇이 언제 고쳐졌는지**만 남습니다. |

### v1.0 범위 밖(아직 없는 것)

- 계정별 로그인·권한 분리
- 시도별 지역 분해 화면 (`IndicatorValue.region` 칸은 **미리 확보만** 해둠)
- 지표 ↔ 이행과제의 강한 연결(현재는 목표·세부목표 단위 연결)
- 자동화 테스트 스위트

---

## 19. 앞으로 할 수 있는 것

| 항목 | 난이도 | 비고 |
|---|---|---|
| 자체 계획지표로 전환 | 낮음 | **코드 수정 없음** — 17.5 절차대로 |
| 지역(시도)별 지표 분해 화면 | 중간 | 데이터 칸은 이미 있음. 화면·필터 추가 필요 |
| 계정별 로그인·권한 | 중간 | 감사 로그에 「누가」를 채우려면 필요 |
| PostgreSQL 전환 | 낮음~중간 | Prisma를 쓰므로 설계도 한 줄 + 접속 주소 변경 수준 |
| 지표↔이행과제 연결 강화 | 중간 | "이 지표를 올리기 위한 과제" 관계 표시 |
| 외부 통계 API 자동 수집 | 높음 | 갱신 주기·출처가 이미 지표에 기록되어 있어 확장 지점은 있음 |

---

## 20. 문서 지도

| 문서 | 읽는 목적 |
|---|---|
| **`OVERVIEW.md`** (이 문서) | 처음 보는 사람에게 전체를 설명 |
| `README.md` | 설치·실행·화면 목록 요약 |
| `PRD.md` | 제품 요구사항 정의서 — 무엇을 왜, 수용 기준, 리스크, 로드맵 |
| `REQUIREMENTS.md` | 재구축용 전체 명세 — 데이터 모델·화면·함정·수용 기준 상세 |
| `PLAN.md` | 설계 결정 기록 — 왜 이렇게 했나, 폐기한 안은 무엇인가 |
| `CLAUDE.md` | 이 저장소에서 작업할 때 지켜야 할 규칙 (AI 세션이 자동으로 읽음) |
| `sheets/README.md` | CSV 컬럼 형식과 입력 규칙 |

---

*이 문서는 2026-08-19 기준 저장소 내용을 직접 확인해 작성했습니다. 화면·수치·파일 경로가 실제와
달라지면 이 문서도 함께 갱신해 주세요.*

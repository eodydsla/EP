# 환경계획(EP) 통합 모니터링 대시보드

| 항목 | 내용 |
|---|---|
| 명칭 | 환경계획 통합 모니터링 대시보드 (EP Dashboard, EP = Environmental Planning) |
| 문서 작성일 | 2026-08-19 |
| 대상 버전 | v1.0 — 모니터링 영역 3분할 + EP 브랜딩 반영본 |
| 저장소 | `/home/dyjin/work/ep_dashboard` (branch `master`) |
| 개발 방식 | Claude Code 기반. 사람이 요구·검수 / AI가 설계·구현·검증 |

---

## 1. 개요

환경계획의 이행 상황을 웹사이트 하나에서 보여주는 대시보드입니다. 지표마다 **기준값 → 최신값 → 목표값**을
넣어두면 사이트가 달성률을 계산해 **달성 / 순조 / 지연 / 악화 / 모니터링 / 자료없음** 6가지 상태로 판정하고
색과 배지로 표시합니다. 담당자는 개발자 없이 관리자 화면에서 지표를 추가하고 실적값을 입력할 수 있으며,
엑셀 표를 CSV로 한 번에 올릴 수도 있습니다. 모든 수정은 이력에 남습니다.

핵심 설계 원칙이 하나 있습니다 — **지금 들어 있는 예시 지표(K-SDGs 환경분야)는 코드 어디에도 박혀 있지
않습니다.** 자체 환경계획 지표가 확정되면 프로그램을 고치지 않고 관리자 화면에서 내용만 갈아끼우면 됩니다.
「목표 / 세부목표 / 지표」라는 **용어 자체도 설정에서 바꿀 수 있습니다.**

---

## 2. 지금 들어 있는 것

| 구분 | 수량 | | 구분 | 수량 |
|---|---:|---|---|---:|
| 모니터링 영역 | 3 | | 연도별 실적값 | 154 (2018~2024) |
| 목표 | 9 | | 이행과제 | 10 (계획이행 영역에만) |
| 세부목표 | 20 | | 사이트 설정 항목 | 12 |
| 지표 | 32 | | 수정 이력 | 16건 (쓸수록 쌓임) |

**모니터링 영역 3개**

| 영역 | 주소 | 색 · 아이콘 | 내용 |
|---|---|---|---|
| 계획이행 모니터링 | `/plan` | 짙은 파랑 `#1864AB` 🧭 | 목표·세부목표별 이행 상황 (지표 13 + 과제 10) |
| 환경상태 모니터링 | `/state` | 청록 `#0CA678` 🌍 | 대기·물·생활/자연환경 관측치 (지표 10) |
| 환경체감 모니터링 | `/perception` | 보라 `#7048E8` 💬 | 시민 만족도·불편·인식 조사 (지표 9) |

```
계획이행    ├ G13 기후변화와 대응   ├ G14 해양생태계 보전   └ G15 육상생태계 보전
환경상태    ├ S1  대기환경          ├ S2  물환경           └ S3  생활·자연환경
환경체감    ├ P1  환경 만족도       ├ P2  환경 불편 경험    └ P3  환경 인식과 참여
```

지표 32개의 상태 분포는 **달성 3 · 순조 11 · 지연 11 · 악화 3 · 모니터링 3 · 자료없음 1**,
전체 평균 달성도는 **43%** 입니다. (예시 데이터 기준 — 13장 참고)

---

## 3. 왜 만들었나

### 3.1 기존 방식의 문제

환경계획 이행 상황은 보통 연 1회 한글/엑셀 보고서로 정리됩니다.

| 문제 | 구체적으로 |
|---|---|
| 열람성 | 기준값·목표값·최신값·달성률이 흩어져 있어 "지금 뭐가 지연 중인지"가 안 보인다 |
| 갱신성 | 통계가 새로 나와도 문서를 다시 만들어야 반영된다 |
| 추적성 | 누가 언제 어떤 수치를 고쳤는지 기록이 남지 않는다 |

### 3.2 참고한 모델

EU JRC의 두 대시보드를 **한 앱에 합쳤습니다.**

| 참고 사이트 | 가져온 것 |
|---|---|
| [EU BDS Actions Tracker](https://dopa.jrc.ec.europa.eu/kcbd/actions-tracker/) | 상태별 집계 카드, 주제별 아코디언, 상태·기한 칩, 요약문 + 근거 링크 |
| [EU BDS 2030 Dashboard](https://dopa.jrc.ec.europa.eu/kcbd/EUBDS2030-dashboard/) | 기준값·목표값·최신값·달성률, 시계열 차트, 메타데이터(출처·산출방법·갱신주기) |

### 3.3 왜 지금은 K-SDGs 예시 데이터인가

자체 계획지표가 아직 확정되지 않아, K-SDGs 환경분야(13 기후변화·14 해양생태계·15 육상생태계)로
**형식 견본**을 먼저 만들어 뒀습니다. 계획지표가 나오면 코드 수정 없이 내용만 교체합니다.

### 3.4 목표

| # | 목표 | 달성 판단 기준 |
|---|---|---|
| G1 | 이행 상황을 한 화면에서 파악 | 첫 화면 3초 안에 영역별 평균 달성도·상태 분포·지연 지표 확인 |
| G2 | 개발자 없이 데이터 갱신 | 담당자가 관리자 화면만으로 지표 추가 → 실적값 입력 → 공개까지 완결 |
| G3 | 지표 체계 교체를 코드 수정 없이 | 계층 명칭 변경 + 데이터 교체만으로 자체 계획지표 대시보드로 전환 |
| G4 | 변경 추적 | 모든 쓰기 작업이 이력에 남음 |

---

## 4. Claude Code로 어떻게 만들었나

### 4.1 역할 분담

| 사람 (발주·검수) | Claude Code (설계·구현) |
|---|---|
| 무엇을 왜 만들지 정의 | 요구를 화면·데이터 구조로 번역 |
| 참고 모델 지정 | 참고 요소를 기능 목록으로 분해 |
| 방향 전환 결정 | 전환에 필요한 스키마·화면 전부 구현 |
| 구조 변경 요구 | 최소 변경으로 확장하는 방법 설계 후 구현 |
| 결과 확인·수용 판단 | 빌드·타입체크·수용기준 검증 수행 및 보고 |
| 공개 여부·수치 검수 책임 | 검수 필요 경고를 문서·화면에 명시 |

**의사결정은 사람이, 구현과 근거 제시는 AI가** 하는 구조입니다.

### 4.2 실제 진행 순서

**① 문제 정의와 참고 모델 조사** — 보고서 방식의 세 문제(열람성·갱신성·추적성)를 정리하고,
EU JRC 두 사이트에서 가져올 요소를 목록화했습니다. **두 사이트를 한 앱에 합친다**가 결론입니다.

**② 자체 앱으로 구축, 단 CSV 경로는 남김** — 관리자 화면이 있는 자체 앱(Next.js + SQLite)으로 만들되,
엑셀·구글시트로 작업한 표를 그대로 올리고 받을 수 있는 CSV 경로를 `/admin/data`에 함께 뒀습니다.
한 건씩 고치는 일은 관리자 화면이 편하지만, 수십 건을 한꺼번에 바꾸는 일은 여전히 CSV가 편하기
때문입니다. 두 방식 중 하나를 고르는 대신 둘 다 남겼습니다.

**③ 뼈대 구축** — 스키마 정의 → `db:push`로 DB 생성 → 시드 스크립트로 초기 데이터 주입.
*(스키마 = DB에 어떤 표가 있고 각 표에 어떤 칸이 들어가는지 적어둔 설계도입니다. 이 프로젝트에서는
`prisma/schema.prisma` 파일 하나가 그 역할을 하고, 그 내용대로 DB 파일이 만들어집니다. 9장 참고)*

**④ 계산 규칙 격리** — 진행률·기대진행률·상태 판정·집계를 `progress.ts` 한 파일에 모았습니다.
화면이 늘어도 계산이 흩어지지 않게 하는 것이 목적입니다.

**⑤ 관리자 화면 구축** — 쓰기는 전부 서버 액션 한 갈래로 모으고, 각 액션이 ① 인증 확인 → ② 작업
→ ③ **이력 기록** → ④ 화면 갱신 → ⑤ 사람이 읽을 결과 메시지 순서를 지키도록 패턴을 고정했습니다.
(DB 중복 오류 → "이미 같은 번호가 존재합니다"로 번역)

**⑥ 검증** — 실제로 서버를 띄워 화면을 요청하고, 저장 폼 제출을 그대로 재현하는 방식으로 수용 기준을
확인했습니다. 이 과정에서 가장 중요한 오류를 잡았습니다.

> 기대진행률을 **현재연도** 기준으로 계산하고 있었는데, 공표가 늦는 지표가 정상 진행 중인데도 전부
> 「지연」으로 찍혔습니다. → **최신 데이터 연도 기준**으로 변경.

이 수정 하나로 샘플 13개 지표 판정이 **「지연 10건」 → 「순조 5 / 지연 5 / 달성 1 / 악화 1 / 모니터링 1」**
로 바뀌었습니다. 실제로 돌려보고 숫자를 확인하지 않았다면 그대로 배포됐을 오류입니다.

**⑦ 영역 3분할** — "메뉴를 계획이행/환경상태/환경체감 3개로 나누자"는 요구를 **새 기능 3개가 아니라
계층의 최상위 축 하나를 얹는 것**으로 해결했습니다. `Track` 표를 추가하고 목표에 소속 영역을 붙이니
아래 구조와 계산은 그대로입니다. 주소는 `[track]` 동적 세그먼트라 영역을 추가하면 메뉴·주소가 자동
생성됩니다. 과제 0건인 영역은 탭을 숨기고, 지표 19개를 추가하되 영역마다 6상태가 고루 나오도록 값을
구성했으며, CSV를 7종으로 확대했습니다. (회귀 29항목 통과)

### 4.3 주의할 점

AI는 요구하지 않으면 그럴듯한 기본값을 채워 넣습니다. 이 프로젝트의 예시 수치도 **공식 통계를 반영하려
했지만 실제 공식 수치와 다를 수 있으므로**, 대외 공개 전 소관 부서 검수가 반드시 필요합니다.

하드코딩 금지 같은 제약은 매번 명시해야 유지되기 때문에 저장소 규칙서(`CLAUDE.md`)에 적어 뒀습니다.
자동 테스트가 없으므로 큰 변경 뒤에는 수용 기준을 다시 훑는 절차가 필요합니다.

---

## 5. 사이트 전체 지도

```
공개 화면
  /                      통합 현황 — 영역 3개 비교
  /plan                  영역 개요            ┐
  /plan/indicators       지표 목록 + 상세 드로어 │ 영역마다 같은 화면 세트가
  /plan/actions          이행과제 트래커        │ 자동으로 생성됨
  /plan/data             데이터·방법론         ┘
  /state , /perception   (동일 구성. 이행과제 0건이라 actions 탭은 숨김)
  /login                 관리자 로그인
  /api/export?type=...   CSV 내려받기

관리자 화면 (/admin, 로그인 필요)
  /admin              개요 — 데이터 규모, 이행 현황, 점검 필요 항목, 최근 이력
  /admin/structure    영역 → 목표 → 세부목표 3단 계층 관리
  /admin/indicators   지표 목록 (+ /new 추가, /[id] 편집·실적값 입력)
  /admin/actions      이행과제 관리
  /admin/data         CSV 가져오기·내보내기
  /admin/config       사이트 설정
  /admin/logs         수정 이력 (최근 300건)
  /admin/preview      미리보기 — 임시저장 항목까지 포함
```

**중요**: `/plan`·`/state`·`/perception` 주소는 프로그램에 적혀 있지 않습니다. DB의 영역 표에 있는 `code`
값에서 옵니다. 관리자에서 영역을 추가하면 **상단 메뉴와 주소가 저절로 늘어납니다.**

---

## 6. 데이터의 4단 계층

```
모니터링 영역 (Track)          예) 환경상태 모니터링       → 주소·상단 메뉴가 됨
   └ 목표 (Goal)               예) S2 물환경               → 색·아이콘을 가짐
        └ 세부목표 (Target)     예) S2-1 하천 수질
             └ 지표 (Indicator) 예) S2-1-1 BOD 농도         → 기준·목표·단위·출처
                  └ 실적값 (IndicatorValue)  2018:2.1  2019:2.0 …

이행과제 (Action) — 목표/세부목표에 연결되는 별도 축. 계획이행 영역에만 존재.
```

**계층 명칭도 데이터입니다.** 화면의 「모니터링 영역」「목표」「세부목표」「지표」라는 단어는 프로그램에
박혀 있지 않고 설정값(`level0_label`~`level3_label`)에서 옵니다.

| 설정 키 | 현재 값 | 바꾼다면 |
|---|---|---|
| `level0_label` | 모니터링 영역 | 분야 |
| `level1_label` | 목표 | 부문 |
| `level2_label` | 세부목표 | 추진전략 |
| `level3_label` | 지표 | 계획지표 |

개발 중 실제로 「부문 / 추진전략 / 계획지표」로 바꿔 화면 전체 용어가 일괄 변경되는 것을 확인했습니다.
이것이 **자체 계획지표 전환 리허설**이었습니다.

---

## 7. 달성도 계산 규칙

계산은 **`src/lib/progress.ts` 한 파일**에만 있습니다. 어느 화면에서 보든 같은 숫자가 나오는 이유입니다.

### 7.1 두 개의 공식

```
진행률(%)     = (최신값 − 기준값) ÷ (목표값 − 기준값) × 100
기대진행률(%) = (최신 데이터 연도 − 기준연도) ÷ (목표연도 − 기준연도) × 100
```

**① 값이 작아져야 좋은 지표도 같은 식으로 됩니다.** 분모 `(목표값 − 기준값)`의 **부호가 방향을 자동으로
흡수**하기 때문입니다. 그래서 방향 설정(up/down)은 계산에 쓰이지 않고 증감 화살표 색과 문구에만 씁니다.

**② 기대진행률은 「현재연도」가 아니라 「최신 데이터 연도」 기준입니다.** 환경 통계는 공표가 1~2년 늦어,
현재연도 기대치와 비교하면 **정상 진행 중인 지표까지 전부 「지연」으로 찍힙니다.** 이 규칙은 바꾸면 안 됩니다.

### 7.2 상태 6종 판정 기준

| 상태 | 조건 | 색 | 의미 |
|---|---|---|---|
| **달성** | 진행률 ≥ 100% | 초록 `#2B8A3E` | 목표값 도달 |
| **순조** | 진행률 ≥ 기대진행률 × 0.9 | 파랑 `#1971C2` | 기대 속도를 따라가는 중 |
| **지연** | 진행률 < 기대진행률 × 0.9 | 주황 `#E8590C` | 기대 속도에 못 미침 |
| **악화** | 진행률 < 0 | 빨강 `#C92A2A` | 기준값보다 후퇴 |
| **모니터링** | 목표값 미설정 | 회색 `#868E96` | 관찰용 지표 |
| **자료없음** | 실적값 없음 | 연회색 `#ADB5BD` | 아직 입력 전 |

- 최신 자료가 **3년**을 넘겨 갱신되지 않으면 「자료 갱신 필요」 배지가 붙습니다.
- 관리자가 상태를 직접 지정하면 자동 판정 대신 그 값을 씁니다.
- 화면 표시 진행률은 0~100%로 자르고, **평균 달성도 계산에서 「자료없음」·「모니터링」은 제외**합니다.

### 7.3 실제 데이터로 계산해 보기

**① 커져야 좋은 지표 — 방재시설 집행 비율 (13-1-1, %)**

```
기준 2020년 72.4   목표 2030년 95.0   최신 2024년 88.1

진행률     = (88.1 − 72.4) ÷ (95.0 − 72.4) × 100 ≈ 69.5%
기대진행률 = (2024 − 2020) ÷ (2030 − 2020) × 100  = 40.0%
69.5% ≥ 40.0% × 0.9(=36.0%)  →  「순조」
```

**② 작아져야 좋은 지표 — 국가 온실가스 총배출량 (13-4-1, 백만 tCO2eq)**

```
기준 2018년 727.6   목표 2030년 436.6   최신 2023년 624.0

진행률     = (624.0 − 727.6) ÷ (436.6 − 727.6) × 100 ≈ 35.6%   ← 음수 ÷ 음수 = 양수
기대진행률 = (2023 − 2018) ÷ (2030 − 2018) × 100 ≈ 41.7%
35.6% < 41.7% × 0.9(=37.5%)  →  「지연」
```

두 지표 모두 **같은 공식 한 줄**로 계산됩니다. 방향을 따로 분기하지 않습니다.

---

## 8. 색상 체계

색이 두 층으로 나뉩니다 — **다양성은 목표별로, 일관성은 상태별로.**

- **목표색(다양성)** — 색을 하나만 지정하면 배경 tint·보더·텍스트·그라디언트 끝색·반투명 3단계가 자동
  파생되어 헤더·액센트 바·도넛·차트·필터 칩까지 전파됩니다. 지정하지 않으면 **10색 팔레트에서 순서대로
  자동 배정**되므로 목표를 몇 개로 늘려도 색이 겹치거나 비지 않습니다.
- **상태색(일관성)** — 6가지 상태색은 전 화면에서 같은 의미로만 쓰고 변주하지 않습니다.
  색맹 대비로 **색 + 아이콘 + 텍스트**를 함께 표기합니다.

---

## 9. 데이터베이스 구조

파일 하나(`prisma/dev.db`)에 표 8개가 들어 있습니다. 설계도는 `prisma/schema.prisma`입니다.

| 표 | 담는 것 | 주요 칸 |
|---|---|---|
| `Track` | 모니터링 영역 | 코드(주소·고유), 이름, 설명, 색, 아이콘, 순서, 공개여부 |
| `Goal` | 목표 | 코드, 소속 영역, 표시번호, 이름, 설명, 색, 아이콘, 순서, 공개여부 |
| `Target` | 세부목표 | 코드, 소속 목표, 이름, 설명, 순서, 공개여부 |
| `Indicator` | 지표 | 코드, 소속 세부목표, 이름, 정의, 산출식, 단위, 방향, 기준·목표(연도/값), 출처·URL, 갱신주기, 담당, 상태 직접지정, 대표지표, 공개여부, 비고, 순서 |
| `IndicatorValue` | 연도별 실적값 | 소속 지표, 연도, 값, 지역(기본 「전국」), 비고 — **(지표+연도+지역)이 고유** |
| `Action` | 이행과제 | 코드, 소속 목표/세부목표, 제목, 요약, 상태, 기한연도, 담당, 최종수정일, 링크, 순서, 공개여부 |
| `Config` | 사이트 설정 | 키, 값 |
| `AuditLog` | 수정 이력 | 시각, 작업, 대상 종류·id·이름, 변경 상세(JSON) |

- **연쇄 삭제**: 영역을 지우면 하위 목표·세부목표·지표·실적값이 함께 사라집니다. 다만 이행과제는
  목표가 사라져도 **과제는 남고 연결만 끊깁니다**(자료 보존).
- **임시저장**: 영역·목표·세부목표·지표·과제 모두 공개여부 플래그가 있어, 감춰도 데이터는 보존됩니다.
- **조회 방식**: 화면을 그릴 때 함수 하나가 계층 전체를 한 번에 읽고, 각 지표에 계산 결과와 상위 정보
  (목표명·색·영역코드)를 **평탄화해 붙여** 내려줍니다.

---

## 10. CSV 왕복

원칙은 하나입니다 — **내보낸 파일을 그대로 다시 올릴 수 있어야 한다.**

| 종류 | 컬럼 |
|---|---|
| 영역 | track_id, track_name, track_desc, color, icon, order, display |
| 목표 | goal_id, track_id, goal_no, goal_name, goal_desc, color, icon, order, display |
| 세부목표 | target_id, goal_id, target_name, target_desc, order, display |
| 지표 | indicator_id, target_id, indicator_name, definition, method, unit, direction, baseline_year, baseline_value, target_year, target_value, source, source_url, update_cycle, custodian, status_override, is_headline, display, note |
| 실적값 | indicator_id, year, value, region, note |
| 이행과제 | action_id, goal_id, target_id, title, summary, status, due_year, responsible, last_update, links |
| 설정 | key, value |

- **가져오기**: 고유번호 기준 **upsert**(있으면 덮어쓰고 없으면 생성), **탭 구분 자동 감지**,
  한 줄이 잘못돼도 전체가 중단되지 않고 "몇 행에서 왜 실패했는지"를 알려줍니다.
- **내보내기**: 엑셀에서 한글이 깨지지 않도록 BOM을 붙이고, `?track=plan`으로 영역별 필터가 됩니다.
- `sheets/` 폴더의 CSV는 **초기 시드이자 형식 견본**입니다. `npm run db:seed`가 이를 읽어 DB를 채웁니다
  (기존 데이터를 전부 지우고 다시 넣습니다).

---

## 11. 기술 스택

| 층 | 기술 | 고른 이유 |
|---|---|---|
| 웹 프레임워크 | **Next.js 15** + **React 19** | 화면과 서버 기능을 한 프로젝트에서. 서버 액션으로 폼 → DB 직결 |
| 언어 | **TypeScript 5** | 실행 전에 형 오류를 잡음 |
| DB | **SQLite** + **Prisma 6** | 파일 하나면 끝. PostgreSQL 전환도 변경 최소 |
| 스타일·UI | **Tailwind CSS v4**, **shadcn/ui v4** | 부품을 소스로 들고 있어 자유롭게 수정 |
| 차트 | **Recharts** | 시계열 그래프. 첫 로딩이 무거워지지 않게 **지연 로딩** |
| 기타 | lucide-react(아이콘), sonner(알림), papaparse(CSV), Noto Sans KR(폰트, 굵기 3종만) | |

**실행 환경**: Node.js 20 이상 권장. (Node 18에서는 `npm run fix:node18`을 한 번 실행해야 빌드됩니다.)

---

## 12. 자주 하는 작업

| 하려는 것 | 방법 |
|---|---|
| **지표 추가** | `/admin/indicators` → 「지표 추가」 → 소속 세부목표·번호·이름·단위·방향 → 기준/목표(연도·값) → 저장 → 편집 화면에서 실적값 입력 → 「공개」 체크 |
| **실적값 갱신** | 지표 편집 화면의 실적값 표에서 연도·값 추가. 최신값·진행률·상태·차트가 즉시 갱신. `/admin/config`의 최종 갱신일도 함께 수정 |
| **엑셀로 일괄 입력** | `/admin/data` → 「실적값」 선택 → 엑셀에서 `indicator_id, year, value, region, note` 복사·붙여넣기 → 올리기 |
| **영역 추가** | `/admin/structure` → 「모니터링 영역 추가」 → 주소 코드(영문 소문자·숫자·하이픈)·이름·아이콘·색. 저장하면 **메뉴와 주소가 자동 생성** |
| **임시로 감추기** | 지표 목록의 눈 아이콘 클릭. 데이터는 남고 공개 화면에서만 사라짐 |

**자체 계획지표로 전환하기 (최종 목표)**

1. `/admin/config`에서 계층 명칭 변경 (예: 부문 / 추진전략 / 계획지표)
2. 사이트 제목·부제·지표 체계 이름·기관명 변경
3. `/admin/data`에서 CSV로 새 계층·지표·실적값 일괄 투입
   (순서: 영역 → 목표 → 세부목표 → 지표 → 실적값 → 이행과제)
4. 예시 데이터 삭제 또는 「기존 데이터를 모두 지우고 넣기」 사용
5. `/admin/preview`에서 확인 후 공개

**이 과정에 프로그램 수정은 없습니다.** 그것이 이 사이트의 설계 목적입니다.

---

## 13. 주의사항과 한계

| 항목 | 내용 |
|---|---|
| **수치는 형식 예시** | `sheets/*.csv`와 현재 DB의 값은 형식 견본이며 실제 공식 통계와 다릅니다. 대외 공개 전 소관 부서 검수·교체가 필요하고, 화면 푸터·README의 경고 문구를 **제거하지 마세요** |
| **배포 전 비밀번호 교체** | `.env`의 `ADMIN_PASSWORD`, `SESSION_SECRET`을 반드시 변경 |
| **값 없음 ≠ 0** | 값이 없으면 빈 칸. `0`은 "실제로 0" |
| **계정 구분 없음** | 공용 비밀번호 1개. "누가"가 아니라 **"무엇이 언제"** 만 남습니다 |

**v1.0 범위 밖**: 계정별 로그인·권한 분리 / 시도별 지역 분해 화면(데이터 칸만 미리 확보) /
지표↔이행과제의 강한 연결 / 자동화 테스트 스위트.

---

## 14. 앞으로 할 수 있는 것

| 항목 | 난이도 | 비고 |
|---|---|---|
| 자체 계획지표로 전환 | 낮음 | **코드 수정 없음** — 12장 절차대로 |
| 지역(시도)별 지표 분해 화면 | 중간 | GIS 기능 + 데이터 스키마 등 추가 필요 |
| 지표↔이행과제 연결 강화 | 중간 | "이 지표를 올리기 위한 과제" 관계 표시 |
| 외부 통계 API 자동 수집 | 높음 | 갱신주기·출처가 이미 기록되어 있어 확장 지점은 있음 |

---

## 15. (참고) 공개 화면 기능

### 15.1 `/` — 통합 현황

| 구역 | 내용 |
|---|---|
| 히어로 | 영역색 띠, 지표 체계 이름, 제목·부제, **전체 평균 달성도**, 전체 지표 수 |
| 전체 이행 현황 | 6가지 상태별 지표 개수 카드 |
| 영역별 카드 | 그라디언트 헤더, 아이콘, 설명, **평균 달성도 도넛**, 목표·지표·과제 수, **상태 분포 막대**, 소속 목표 목록 |
| 관리가 필요한 지표 | 전 영역의 지연·악화 지표를 진행률 낮은 순 최대 8개. 클릭하면 상세가 바로 열림 |

카드는 고정 칸 수가 아니라 자동 배치라 영역이 2개든 5개든 한 줄을 채웁니다.

### 15.2 `/[track]` — 영역 개요

히어로(영역명·평균 달성도) → 상태별 집계 → **목표별 카드**(도넛·지표 수·상태 분포, 클릭 시 그 목표만 필터)
→ 관리가 필요한 지표 5개 / 목표에 근접한 지표 5개 → (과제가 있으면) 이행과제 요약.

### 15.3 `/[track]/indicators` — 지표 탐색

가장 많이 쓰게 될 화면입니다.

| 기능 | 동작 |
|---|---|
| 목표 칩 | 목표색이 반영된 칩 + 지표 수 |
| 상태 칩 | 6상태 배지로 필터, 다시 누르면 해제. 0건인 상태는 칩이 안 나옴 |
| 검색 | 지표번호·이름·정의·세부목표·목표·담당·출처 통합 검색 |
| 정렬 | 번호순(기본) / 달성률 높은 순 / 낮은 순 / 최신 연도순 |
| 보기 | 카드 격자 ↔ 목록(표) 전환. 번호순일 때만 세부목표별 그룹 헤더 표시 |

**지표 카드**: 목표색 액센트 바, 상태 배지, 최신값·단위, 전년 대비 증감(방향 설정에 따라 초록/빨강),
진행률 막대와 %, 기준 → 목표, **미니 시계열 스파크라인**, 3년 넘게 갱신 없으면 「자료 갱신 필요」 배지.

**상세 드로어**(카드 클릭 시 오른쪽에서 슬라이드): 시계열 차트, 정의·산출식·단위·방향, 기준·목표,
출처(링크), 갱신주기, 담당, 비고, 상태 판정 근거, **이 지표 원자료만 CSV 내려받기**.

**딥링크**: `?goal=<id>`는 그 목표만 필터, `?indicator=<id>`는 **상세 드로어가 열린 채로** 시작합니다.

### 15.4 `/[track]/actions` — 이행과제 트래커

지표(숫자)와 별개로 **무엇을 하고 있는지**를 추적합니다. 상태 4종(**완료·추진중·지연·예정**),
상태별 집계, 상태·목표·기한연도 필터와 검색, 목표별 아코디언, 요약문 + **근거 링크**, 담당·기한·최종 수정일.
**과제가 0건인 영역에서는 이 탭이 아예 나타나지 않습니다** — 관측·조사만 하는 영역에 빈 트래커를
남기지 않으려는 의도적 설계입니다.

### 15.5 `/[track]/data` — 데이터·방법론

달성도 계산식과 6상태 판정 기준(7장 내용) + 해당 영역 **지표 메타데이터 전체 표**(번호·이름·정의·단위·
기준·최신·목표·출처·갱신주기·담당·상태) + 영역별 CSV 내려받기.

---

## 16. (참고) 관리자 화면 기능

`/admin` 이하는 로그인해야 보입니다. 미로그인 접근은 `/login`으로 넘어갑니다.

### 16.1 `/admin` — 개요
데이터 규모 카드 · 이행 현황(임시저장 포함) · **점검이 필요한 항목**(임시저장 / 실적값 없음 / 3년 이상
미갱신 / 목표값 없음) · 최근 수정 이력 8건.

### 16.2 `/admin/structure` — 계층 관리
영역 → 목표 → 세부목표 3단을 아코디언에서 추가·수정·삭제. 삭제 확인창은 "하위 세부목표 n개, 지표 m개가
함께 사라집니다"까지 알려줍니다. 세부목표 추가 시 **다음 코드 자동 제안**(13-1, 13-2 → 13-3),
목표의 **상위 영역을 바꾸면 다른 영역으로 이동**, 색을 비우면 자동 배정.
영역 코드는 주소로 쓰이므로 영문 소문자·숫자·하이픈만 허용합니다.

### 16.3 `/admin/indicators` — 지표 목록
영역·목표·세부목표·상태·공개여부 필터와 검색. **눈 아이콘 클릭으로 공개 ↔ 임시저장 즉시 전환.**

### 16.4 `/admin/indicators/[id]` — 지표 편집
**① 메타데이터** — 번호, 소속 세부목표, 이름, 정의, 산출식, 단위, 방향, 기준연도·값, 목표연도·값,
출처·URL, 갱신주기, 담당, 비고, 대표지표, 공개여부, 순서, **상태 직접 지정**(자동 판정을 덮어씀).
**② 연도별 실적값 인라인 편집** — 표에서 바로 행 추가·수정·삭제. 연도는 다음 해를 미리 채워주고
지역(기본 「전국」)·비고(「잠정치」 등)도 입력. 값을 넣으면 **최신값·진행률·상태·차트가 즉시 재계산**됩니다.

> 값이 없으면 빈 칸으로 두세요. `0`은 "실제로 0"이라는 뜻입니다.

### 16.5 `/admin/actions` — 이행과제 관리
번호, 소속 목표/세부목표, 제목, 요약, 상태, 기한 연도, 담당, 최종 수정일, 근거 링크(`제목|URL;…`), 공개여부.

### 16.6 `/admin/data` — CSV 가져오기·내보내기
7종 내려받기(건수 표시) / 파일 업로드 또는 **엑셀·구글시트에서 복사해 붙여넣기**(탭 구분 자동 인식) /
「기존 데이터를 모두 지우고 넣기」 옵션 / **업로드 순서 안내**(영역 → 목표 → 세부목표 → 지표 → 실적값 →
이행과제 → 설정) / 컬럼 형식 안내표.

### 16.7 `/admin/config` — 사이트 설정
제목·부제·**계층 명칭 4종**·지표 체계 이름·기관명·문의처·최종 갱신일·푸터 문구. 새 설정 키도 추가 가능.

### 16.8 `/admin/logs` — 수정 이력
최근 300건. 시각 / 작업(추가·수정·삭제·가져오기) / 대상 / 이름 / **변경 상세(JSON)**.

### 16.9 `/admin/preview` — 미리보기
공개 화면과 같은 부품으로 렌더하되 **임시저장 항목까지 포함**해 보여줍니다(배지 표시). 공개 전 최종 확인용.
